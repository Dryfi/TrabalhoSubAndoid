<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.trabalhosub">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TrabalhoSub">

        <!-- Atividade principal do aplicativo -->
        <activity android:name=".view.CadastroAlunoActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Outras atividades do aplicativo -->
        <activity android:name=".view.MainActivity" android:exported="false" />
        <activity android:name=".view.ListaAlunosActivity" android:exported="false" />
        <activity android:name=".view.RelacaoMediasActivity" android:exported="false" />
        <activity android:name=".view.RelacaoNotasActivity" android:exported="false" />

    </application>

</manifest>
